# Keep Omarchy no-gaps mode, but restore a minimal active focus border.
# Loaded after Omarchy toggle flags so it wins over dynamic toggles.
general {
    border_size = 1

    # Let the active border come from the current theme. Hide inactive borders.
    col.inactive_border = rgba(00000000)
}
